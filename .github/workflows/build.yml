name: Build

permissions:
  contents: read
  id-token: write

on:
  push:
  pull_request:
    branches:
      - main
jobs:
  spring-boot-3:
    name: SpringBoot3
    uses: ./.github/workflows/check.yml
